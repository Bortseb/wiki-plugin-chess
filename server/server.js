/**
 * Wiki server plugin entry — federated wiki loads `server/server.js` by convention.
 * Implementation lives in puzzle-server.js.
 */

// # Plugin Server Entry

export * from './puzzle-server.js'
